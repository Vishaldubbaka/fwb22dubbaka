extends layout

block content
  h1= title 
  - var image_names = ["suit1.PNG","suit2.PNG","suit3.PNG","suit4.PNG","suit5.PNG"];
  - for(let i=0; i<3; i++){
    - let number = Math.floor(Math.random()*5);
    img(src = `/images/${image_names[number]}` alt = `Image No ${image_names[number]}`)
    -}
